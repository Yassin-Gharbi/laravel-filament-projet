<div class="border rounded shadow p-4 bg-white">
    <h3 class="text-lg font-semibold">{{ $property->name }}</h3>
    <p class="mt-2 text-gray-600">{{ $property->description }}</p>
    <p class="mt-4 font-bold">{{ $property->price_per_night }} € / nuit</p>
</div>
